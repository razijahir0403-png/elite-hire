# Production (Vercel) — set this in Vercel → Project → Settings → Environment Variables
# Replace with your Render backend URL (must end with /api)
VITE_API_BASE_URL=https://your-service-name.onrender.com/api
